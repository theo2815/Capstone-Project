upstream api {
    server ai-api:8000;
}

# Rate limiting zone
limit_req_zone $binary_remote_addr zone=api_limit:10m rate=30r/s;

server {
    listen 80;
    server_name _;

    # For Let's Encrypt certificate verification
    location /.well-known/acme-challenge/ {
        root /var/lib/letsencrypt;
    }

    # Redirect all other HTTP traffic to HTTPS (enable after SSL setup)
    # location / {
    #     return 301 https://$host$request_uri;
    # }

    # Temporary: serve directly on HTTP until SSL is configured
    location / {
        proxy_pass http://api;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # File upload limits (matches MAX_FILE_SIZE * MAX_BATCH_SIZE)
        client_max_body_size 200M;

        # Timeouts for ML inference
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
        proxy_connect_timeout 10s;

        # Rate limiting
        limit_req zone=api_limit burst=50 nodelay;
    }

    # Health check (no rate limit)
    location /api/v1/health {
        proxy_pass http://api;
        proxy_set_header Host $host;
        limit_req off;
    }
}

# HTTPS server block - uncomment after running certbot
# server {
#     listen 443 ssl http2;
#     server_name YOUR_DOMAIN;
#
#     ssl_certificate /etc/letsencrypt/live/YOUR_DOMAIN/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/YOUR_DOMAIN/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     location / {
#         proxy_pass http://api;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#
#         client_max_body_size 200M;
#         proxy_read_timeout 120s;
#         proxy_send_timeout 120s;
#         proxy_connect_timeout 10s;
#
#         limit_req zone=api_limit burst=50 nodelay;
#     }
#
#     location /api/v1/health {
#         proxy_pass http://api;
#         proxy_set_header Host $host;
#         limit_req off;
#     }
# }
