---
paths:
  - "website/**"
  - "desktop/**"
---

# Frontend / Desktop UI — Design Skill (MANDATORY)

Any task that creates, redesigns, or polishes **web or desktop** UI/UX — components, pages, layouts, styling, animations, design systems — is governed by the **Frontend Design** skill. It overrides default approaches (Tailwind / shadcn defaults).

**Read it in full before producing any UI work it covers:**
`C:\Users\Theo Cedric Chan\Documents\Obsidian Vault\QuickPitik Vault\Claude Skills\Frontend Design.md`

Applies to `website/` and `desktop/`. Does **NOT** apply to `mobile/` — that uses Mobile Design (`.claude/rules/mobile-design.md`).

**Non-negotiable website UI pre-flight:** open the route at **375 px**, then confirm **`md` (≥768)** and **`lg` (≥1024)**. The website's live design tokens/direction are tracked in the vault at `website/notes/design-system.md` — read it for current tokens rather than assuming.
